# 2026-09 系統日誌

> 格式：Changelog + 決策推論
> 記錄本月對 Vault 系統所做的新增、調整、刪減，及其背後原因

---

## 📊 月份快照

| 項目 | 月初 | 目前 |
|------|------|------|
| Projects/ 子專案 | claude-bot | claude-bot、**PMP** |
| 2026 年計畫 KR 週期 | Q2（已過期 4 個月） | **Q4（9–12 月）** |
| PMP 備考狀態 | 讀完 PMBOK 6、未報名 | 計畫重建、待報名課程 |

---

## ➕ 新增（9/5）

### Projects/PMP/ — PMP 備考主計畫

**做了什麼：**
- 新增 `Projects/PMP/PMP 備考主計畫.md`：含改版事實、費用與考試次數規則、申請注意事項、14 週時間軸、讀書節奏、方法論、模擬考記錄表、錯題本結構、防過度準備護欄
- 更新 `靈感筆記/2026年計畫.md`：
  - O1 的 PMP 段落全面改寫（舊行動項「看講義」「看完《淺出 PMP》」標記停用）
  - 考期由「2026 年 9 月」改為「約考 12/13 / 截止線 12/31」
  - 費用 544 USD 更正（PMI 於 2026/8/6 調漲：非會員 675 / 會員 445）
  - Q2 KR 摺疊保留並標記未完成原因，新增 Q4 KR

**推論：**

1. **考試已改版是整件事的前提。** 2026/7/9 起 PMP 換新版 ECO，Business Environment 由 8% → 26%，PMBOK 已出第八版（2025/11）。Wilson 讀的第六版落後兩個世代，原計畫的行動項全部失效。不先確認這件事，後續所有規劃都會建立在錯誤前提上。

2. **真正的卡點不是知識，是 35 contact hours。** 自讀 PMBOK 不計入 contact hours，而它是報名硬門檻。Q2 KR3 至今未完成 —— 這是原訂 9 月考期跳票的真正根因。計畫因此把「報名課程繳費」定為唯一的下一步行動，而非任何讀書任務。

3. **計畫刻意設計成對抗過度準備。** 依 CLAUDE.md 的思維模型，Wilson 的風險是「分析 > 行動、把準備當成進度」。本次跳票正是該模式的實證（讀了 700 頁參考書，卻沒完成報名門檻）。因此：
   - 出考場判準寫成硬數字（連續 3 次 ≥ 75%），定位為**終點線而非待優化參數**
   - 加入「不再新增學習資源」「考試日期先於準備程度」等護欄
   - 要求「申請一核准就立刻約考」，用已付費的日期反推準備

4. **節奏設計預留失敗空間。** 平日規劃 4 天而非 5 天（第 5 天當補課日）、設 25 分鐘下限而非只設 90 分鐘目標 —— 對容易因斷鏈而自我否定的人格，讓計畫預先容納失敗比追求全勤更穩。

5. **方法論綁定既有技能以降低啟動阻力。** 將錯題本設計為 4 行結構並要求每兩週做 EDA，把備考轉譯成 Wilson 的本職工作（數據分析）語言，同時解決「錯題本寫成長篇心得而無法統計」的常見失敗。

---

## 🔧 修正（9/9）

### notify.py：TARGET_DATE 未貫穿全域

**改了什麼：**
- `notify.py` 三處 `datetime.now()` 改為 `_now`：
  - L118 `collect_context()` — 回溯過去 7 天日報的基準日
  - L161 `build_quest_section()` — 「昨日 +X pt」的取數日
  - L180 `get_weekly_done()` — ISO 週次計算
- 備份於 `/tmp/notify.py.bak`

**推論：**

L16 已支援 `TARGET_DATE` 環境變數覆寫日期，但只套用到 `TODAY` / `WEEKDAY` / `DOW` 三個常數，其餘三處仍讀真實系統時間。正常每日排程時兩者相同、不會顯現；只有在**補跑過去日期**時才會露出：補跑 9/8 會抓到以 9/9 為基準的前七天與「昨日」資料，產生張冠李戴的進度行。既然 `TARGET_DATE` 的存在目的就是補跑，這三處屬於實作遺漏而非設計取捨，直接對齊 `_now`。

---

## 🔁 補跑（9/9）

### 補產 9/8、9/9 Daily Note

**做了什麼：**
- 以 `TARGET_DATE=2026-09-08`、`TARGET_DATE=2026-09-09` 依序執行 `notify.py`
- 依序執行是必要的：`notify.py` 會改寫 `主頁.md` 的今日連結，先補舊日期才能讓主頁最終停在 9/9

**根因：WSL 未在排程時間運行。**
- `uptime -s` 顯示 WSL 本次開機時間為 2026-09-09 21:16，即本次對話喚起 WSL 的時刻
- `/tmp/*.log` 全數不存在（WSL 關機時 /tmp 清空），佐證 9/8、9/9 兩日 08:00 時 WSL 並未運行
- cron.service 本身 `enabled` 且運作正常，非服務故障

**待觀察：** README 記載「Windows 啟動時自動觸發」，但實際未在 8:00 前喚起 WSL。若後續仍反覆漏跑，需檢討觸發方式（例如改用 Windows 工作排程器喚起 WSL，或改為開機後補跑而非固定時刻）。

---

## 📌 待補

- 8 月系統日誌缺漏（本月未補；如需回溯再處理）
- Daily Note 漏跑的根本解：WSL 未於 08:00 運行，觸發方式待檢討
